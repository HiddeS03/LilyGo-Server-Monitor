/**
 * WiFi Credentials for Game Server Monitor
 * 
 * This file contains sensitive WiFi credentials and is gitignored.
 * Copy credentials.h.example to credentials.h and fill in your details.
 */

#ifndef CREDENTIALS_H
#define CREDENTIALS_H

// WiFi Configuration
const char *WIFI_SSID = "YOUR_WIFI_SSID";
const char *WIFI_PASSWORD = "YOUR_WIFI_PASSWORD";

// Server Configuration (your Docker host IP)
const char *SERVER_URL = "http://192.168.1.XXX:5000/status";

#endif // CREDENTIALS_H
